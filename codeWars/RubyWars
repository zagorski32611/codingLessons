def is_square(x)
  if Math.sqrt(x) % 1 != 0
    false
  else true
  end
end
puts is_square(6)
puts is_square(36)
puts is_square("rice")

# this is the correct answer:

def is_square(x)
  x < 0 ? false : Math.sqrt(x) % 1 == 0
end
